{
  // Server authentication info
  "servers": [
    {
      "host": "104.236.211.140",
      "username": "root",
      // or pem file (ssh based authentication)
      "pem": "~/.ssh/id_rsa"
    }
  ],
  "buildOptions": { // optional
    "debug": true
  },

  // Install MongoDB on the server. Does not destroy the local MongoDB on future setups
  // "setupMongo": true,

  // WARNING: Node.js is required! Only skip if you already have Node.js installed on server.
   "setupNode": true,

  // WARNING: nodeVersion defaults to 0.10.36 if omitted. Do not use v, just the version number.
   "nodeVersion": "4.4.7",

  // Install PhantomJS on the server
  // "setupPhantom": false,

  // Show a progress bar during the upload of the bundle to the server.
  // Might cause an error in some rare cases if set to true, for instance in Shippable CI
  "enableUploadProgressBar": true,

  // Application name (no spaces).
  "appName": "pickk",

  // Location of app (local directory). This can reference '~' as the users home directory.
  // i.e., "app": "~/Meteor/my-app",
  // This is the same as the line below.
  "app": "~/desktop/pickk",

  // Configure environment
  // ROOT_URL must be set to https://YOURDOMAIN.com when using the spiderable package & force SSL
  // your NGINX proxy or Cloudflare. When using just Meteor on SSL without spiderable this is not necessary
  "env": {
    "PORT": 80,
    "ROOT_URL": "http://stag.pickk.net",
    "MONGO_URL": "mongodb://getpickk:xEZMosrpQRqg@candidate.11.mongolayer.com:11179,candidate.43.mongolayer.com:11034/pickk-stag?replicaSet=set-55dbc8150df18460e00000cf",
    "MONGO_OPLOG_URL": "mongodb://getpickk:xEZMosrpQRqg@candidate.11.mongolayer.com:11179,candidate.43.mongolayer.com:11034/local?authSource=pickk-stag&replicaSet=set-55dbc8150df18460e00000cf",
    "MAIL_URL": "smtp://postmaster%40mg.pickk.co:2e53ee0dce2deade7b63443c39322243@smtp.mailgun.org:587"
  },

  // Meteor Up checks if the app comes online just after the deployment.
  // Before mup checks that, it will wait for the number of seconds configured below.
  "deployCheckWaitTime": 300
}
