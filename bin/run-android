#!/bin/bash

set -eux

DIRNAME=$(cd `dirname $0` && pwd)
ENV=${1:-}
if [ -z $ENV ]; then
  ENV="prod"
else
  shift
fi

echo $ENV

$DIRNAME/merge-settings $ENV android > /tmp/$ENV.json
# --mobile-server https://pickk.net

meteor run android-device --settings settings.json
# /tmp/$ENV.json "$@"
