{
  "name": "pickk",
  "version": "1.0.0",
  "description": "Script to make sure process are done correctly.",
  "scripts": {
    "start": "echo 'Please execute `./bin/run`'",
    "ios-test": "echo 'Please execute `./bin/run ios`'",
    "production": "echo 'Please execute `./bin/build-ios`'"
  },
  "dependencies": {
    "bcrypt": "^0.8.7",
    "mupx": "^1.5.3"
  },
  "devDependencies": {
    "mkpath": "^1.0.0",
    "node-version-compare": "^1.0.1",
    "plist": "^2.0.1",
    "xml2js": "^0.4.17"
  }
}
